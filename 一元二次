# -*- coding: utf-8 -*-
import math
def quadratic(a, b, c):
    delt=b*b-4*a*c
    if delt<0:
        print("无解")
    elif delt==0:
	    print(-b/(2*a))
    else:
	    return (math.sqrt(delt)-b)/(2*a),(-math.sqrt(delt)-b)/(2*a)
print(quadratic(2,3,1))
print(quadratic(1,3,-4))
